# data.go.kr > 전국관광지정보표준데이터 > Open API 탭 > 활용신청 후 발급되는 일반 인증키(디코딩 키)
DATA_GO_KR_TRRSRT_API_KEY=

# Firebase 콘솔 > 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성 으로 받은 JSON 파일의 경로
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# 한 번에 수집할 최대 건수 (기본 50 — 전체 852건을 한 번에 넣기 전에 소량으로 먼저 검증 권장)
COLLECT_LIMIT=50
